version: "3.9"

services:
  web:
    build: .
    ports:
      - "80:5000"
    volumes:
      - .:/app       # ソースコード変更でホットリロード
      - bbs_data:/app/bbs.db  # DB永続化
    environment:
      - FLASK_APP=app.py
      - FLASK_RUN_HOST=0.0.0.0
      - FLASK_ENV=development

volumes:
  bbs_data: